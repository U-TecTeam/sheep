# Sheep

这是一个使用 **Next.js + Tailwind CSS** 的基础项目。

## 开始开发

```bash
npm install
npm run dev
```

打开 `http://localhost:3000` 查看页面。

## 可用脚本

- `npm run dev`：启动开发服务器
- `npm run build`：构建生产版本
- `npm run start`：启动生产服务器
- `npm run lint`：运行 ESLint
